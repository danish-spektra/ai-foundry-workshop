# ── Multi-agent frameworks (Exercise 6) ──
# Semantic Kernel requires exactly 1.53.1 for the New Foundry (endpoint-based AzureAIAgent).
semantic-kernel[azure]==1.53.1

# AutoGen 0.4.x. The AutoGen notebook uses autogen_ext.models.openai.AzureOpenAIChatCompletionClient,
# so the OpenAI extra is required. (Do not add the legacy 0.2 `autogen` package — it conflicts.)
autogen-agentchat
autogen-core
autogen-ext[openai]
nest_asyncio                              # Allow asyncio.run() inside Jupyter cells
